export const typeDefs = `

  type Student {
    id: ID!
    name: String!
    age: Int!
    email: String!
  }

  type Query {
    hello: String
    students: [Student]
  }

  type Mutation {
    createStudent(name: String!, age: Int!, email: String!): Student
  }

`;